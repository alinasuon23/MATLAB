%Alina Suon / October 1, 2018 / Lab 2

%define the function poly(a,x) which takes as input a vector
%a=[a_1,a_2,...a_n] and a real number x, and returns value:
%a_1x^(n-1)+z_2x^(n-2)+...+a_(n-1)x+a_n.
%1. prompt user to input an array a of coefficients, a real number x, then
%display poly (a,x).


%prompts user for an array of coefficent
a = input ('Input an array of coefficients: ');     %variable for coefficients, in array a
x = input ('Input a real number: ');                %variable for real number, x





%put coefficient into a list array

%poly(a,x) = a(1);

%disp(a)
%fprintf('poly('a','x') = a(0)*x

% fprintf('a[%d] = %f\n', n, x(n) );

%A = [a];
%n = numel(A)


